apiVersion: v1
kind: Pod
metadata:
  name: redis-DB
  labels:
    name: redis-pod
    app: voting-app
    type: DB
spec:
  containers:
    - name: redis
      image: redis
      ports:
        - containersPort: 6379