apiVersion: v1
kind: Service
metadata:
  name: result-service
  labels:
    name: result-service
    app: voting-app
    type: frontend
sepc:
  ports:
    - port: 80 
      targetPort: 80
  selector:
    name: voting-app-result